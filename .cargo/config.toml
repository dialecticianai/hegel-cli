[alias]
# Build and install in one command (uses cargo install, slower)
bi = "install --path . --force"

# Build release and install (uses cargo install with lockfile)
install-local = "install --path . --force --locked"
